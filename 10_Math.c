#include <stdio.h>
#include <math.h>

int main() {
    double a = 3.14;

    printf("%lf\n", ceil(a)); // làm tròn lên
    printf("%lf\n", floor(a)); // làm tròn xuống
    printf("%lf\n", log10(10000));
    printf("%d\n", (int)log10(10000)); // ép kiểu
}